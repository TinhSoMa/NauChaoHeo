import { OpenRouterSettings } from '../openrouter/OpenRouterSettings'

export function OpenRouterDetail() {
  return (
    <div style={{ maxWidth: 'none', width: '100%' }}>
      <OpenRouterSettings />
    </div>
  )
}
